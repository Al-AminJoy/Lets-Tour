<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.LetsTour" parent="Theme.MaterialComponents.Light.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/basicColor</item>
        <item name="colorPrimaryVariant">@color/basicColor</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/basicColor</item>
        <item name="colorSecondaryVariant">@color/basicColor</item>
        <item name="colorOnSecondary">@color/white</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">@color/basicLighter</item>
        <!-- Customize your theme here. -->
    </style>
    <style name="ProgressColor" parent="Theme.AppCompat.Light.Dialog.Alert">
        <item name="colorAccent">@color/basicColor</item>
    </style>
</resources>